# 核心代码讲解与 JavaFX 运用

本文档详细介绍中国象棋项目的核心代码架构，以及 JavaFX 在本项目中的具体运用方式。

## 目录

- [架构概览](#架构概览)
- [核心引擎层](#核心引擎层)
- [AI 引擎层](#ai-引擎层)
- [UI 层与 JavaFX 运用](#ui-层与-javafx-运用)
- [数据层](#数据层)

---

## 架构概览

项目采用经典的 **MVC + 观察者模式** 架构：

```
┌─────────────────────────────────────────────────────────────┐
│                         UI 层                                │
│  ChineseChessApp → BoardView → GameController → AIIntegration│
└──────────────────────────┬──────────────────────────────────┘
                           │ 观察者模式
┌──────────────────────────▼──────────────────────────────────┐
│                       核心引擎层                              │
│  GameEngine → MoveGenerator → CheckDetector → 各棋子规则类    │
└──────────────────────────┬──────────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────┐
│                        数据层                                │
│              AppSettings → GameRecordManager                 │
└─────────────────────────────────────────────────────────────┘
```

---

## 核心引擎层

### 1. 游戏引擎 (GameEngine)

**文件位置**: `src/main/java/cn/chinesechess/core/engine/GameEngine.java`

游戏引擎是整个应用的核心，负责管理游戏状态、处理走棋逻辑、判定胜负。

**核心职责**:
- 走法验证与执行
- 回合管理
- 将军/将死/困毙检测
- 三次重复局面判和
- 事件通知（观察者模式）

**关键代码片段** - 走棋执行与状态检测:

```java
@Override
public MoveResult makeMove(Position from, Position to) {
    // 1. 获取棋子并验证归属
    Piece piece = board.getPiece(from);
    if (piece == null || piece.getColor() != currentTurn) {
        return MoveResult.INVALID;
    }

    // 2. 验证目标位置合法性
    List<Position> legalTargets = getLegalTargets(from);
    if (!legalTargets.contains(to)) {
        return MoveResult.INVALID;
    }

    // 3. 执行走棋（命令模式）
    MoveCommand command = new MoveCommand(move, piece);
    command.execute(board);
    moveHistory.add(command);

    // 4. 检测游戏状态
    if (checkDetector.isCheckmate(board, nextTurn)) {
        result = MoveResult.CHECKMATE;
        gameResult = (nextTurn == Color.RED) ? GameResult.BLACK_WIN : GameResult.RED_WIN;
    } else if (checkDetector.isInCheck(board, nextTurn)) {
        result = MoveResult.CHECK;
    }
    // ...

    // 5. 通知 UI 层
    notifyMoveMade(move, result);
    notifyStateChanged();
    return result;
}
```

### 2. 走法生成器 (MoveGenerator)

**文件位置**: `src/main/java/cn/chinesechess/core/rules/MoveGenerator.java`

负责生成所有棋子的合法走法，是象棋规则的核心实现。

**设计模式 - 策略模式**:
- 每个棋子类型对应一个 `IMoveRule` 实现类
- 通过 `MoveRuleFactory` 工厂类创建对应的规则处理器

```java
// 走法生成核心逻辑
public List<Move> getLegalMoves(IBoard board, Color color) {
    List<Move> legalMoves = new ArrayList<>();

    // 遍历当前方所有棋子
    for (Piece piece : board.getPiecesByColor(color)) {
        // 获取对应棋子的规则处理器
        IMoveRule rule = MoveRuleFactory.getRule(piece.getType());
        // 生成该棋子的所有合法走法
        List<Position> targets = rule.getLegalTargets(board, piece);
        for (Position target : targets) {
            // 模拟走棋，验证是否会导致己方被将军
            if (!wouldCauseSelfCheck(board, piece, target)) {
                legalMoves.add(new Move(piece.getPosition(), target, captured));
            }
        }
    }
    return legalMoves;
}
```

### 3. 棋子规则类

各棋子规则实现位于 `cn.chinesechess.core.rules` 包:

| 类名 | 对应棋子 | 规则特点 |
|------|----------|----------|
| `RookMoveRule` | 车 | 直线运动，无障碍 |
| `KnightMoveRule` | 马 | 日字形，"蹩马腿"检测 |
| `CannonMoveRule` | 炮 | 直线运动，吃子需隔子 |
| `PawnMoveRule` | 兵/卒 | 过河前后走法不同 |
| `KingMoveRule` | 将/帅 | 九宫内移动 |
| `AdvisorMoveRule` | 士/仕 | 斜向，九宫内 |
| `ElephantMoveRule` | 象/相 | 田字形，"塞象眼"检测 |

---

## AI 引擎层

### 1. AI 接口设计

**文件位置**: `src/main/java/cn/chinesechess/ai/IAIEngine.java`

定义 AI 引擎的统一接口，支持多种引擎实现:

```java
public interface IAIEngine {
    /** 搜索最佳走法 */
    Move searchBestMove(IBoard board, Color color, int depth);

    /** 设置搜索深度（难度） */
    void setDepth(int depth);

    /** 停止搜索 */
    void stop();
}
```

### 2. 内置 Minimax 引擎

**文件位置**: `src/main/java/cn/chinesechess/ai/MinimaxEngine.java`

实现了带 Alpha-Beta 剪枝的 Minimax 算法:

```java
@Override
public Move searchBestMove(IBoard board, Color color, int depth) {
    // 评估函数 + Minimax 递归搜索
    Move bestMove = null;
    int bestScore = color == Color.RED ? Integer.MIN_VALUE : Integer.MAX_VALUE;

    for (Move move : moveGenerator.getLegalMoves(board, color)) {
        // 1. 执行走棋
        board.makeMove(move);

        // 2. 递归搜索
        int score = minimax(board, depth - 1, alpha, beta,
                color.opposite(), false);

        // 3. 撤销走棋
        board.undoMove();

        // 4. 更新最优走法
        if (color == Color.RED ? score > bestScore : score < bestScore) {
            bestScore = score;
            bestMove = move;
        }

        // 5. Alpha-Beta 剪枝
        if (color == Color.RED) {
            alpha = Math.max(alpha, bestScore);
        } else {
            beta = Math.min(beta, bestScore);
        }
        if (beta <= alpha) break; // 剪枝
    }
    return bestMove;
}
```

### 3. 局面评估 (BoardEvaluator)

**文件位置**: `src/main/java/cn/chinesechess/ai/BoardEvaluator.java`

静态局面评估函数，考虑:
- 棋子价值分（车>马>炮>兵...）
- 棋子位置分（各棋子在不同位置的分值）
- 灵活性分（可走步数）
- 威胁与保护关系

---

## UI 层与 JavaFX 运用

### 1. 应用入口 (ChineseChessApp)

**文件位置**: `src/main/java/cn/chinesechess/ui/ChineseChessApp.java`

继承 `javafx.application.Application`，是 JavaFX 应用的入口点:

```java
public class ChineseChessApp extends Application {

    @Override
    public void start(Stage primaryStage) {
        // 1. 创建游戏引擎和视图
        IGameEngine gameEngine = new GameEngine();
        BoardView boardView = new BoardView();
        GameController controller = new GameController(gameEngine, boardView, engineManager);

        // 2. 创建布局 (BorderPane: 上-中-下结构)
        BorderPane root = new BorderPane();
        root.setCenter(boardView);
        root.setTop(menuBar);
        root.setBottom(statusBar);

        // 3. 创建场景并加载 CSS
        Scene scene = new Scene(root);
        scene.getStylesheets().add(getClass().getResource("/css/style.css").toExternalForm());

        // 4. 注册键盘快捷键
        scene.getAccelerators().put(
            new KeyCodeCombination(KeyCode.N, KeyCombination.CONTROL_DOWN),
            () -> showNewGameDialog(controller, owner)
        );

        primaryStage.setScene(scene);
        primaryStage.show();
    }
}
```

**JavaFX 特性运用**:
- `Application` - JavaFX 应用基类
- `Stage` / `Scene` - 窗口和场景
- `BorderPane` - 边框布局
- `MenuBar` - 菜单栏
- `ToolBar` - 工具栏
- `Accelerator` - 键盘快捷键

### 2. 棋盘视图 (BoardView)

**文件位置**: `src/main/java/cn/chinesechess/ui/BoardView.java`

使用 `Canvas` 实现棋盘绘制，继承 `StackPane`:

```java
public class BoardView extends StackPane {

    private final Canvas canvas;
    private IBoard board;

    public BoardView() {
        canvas = new Canvas(BOARD_WIDTH, BOARD_HEIGHT);
        getChildren().add(canvas);
    }

    // 绘制棋盘网格
    private void drawBoard(GraphicsContext gc) {
        // 绘制9条横线
        for (int row = 0; row <= 9; row++) {
            gc.strokeLine(x1, y, x2, y);
        }
        // 绘制10条竖线（楚河汉界处断开）
        // 绘制九宫对角线
        // 绘制兵/炮位置标记
    }

    // 绘制棋子
    private void drawPieces(GraphicsContext gc) {
        for (Piece piece : board.getAllPieces()) {
            // 绘制棋子圆底
            gc.setFill(Color.rgb(255, 248, 220));
            gc.fillOval(x - radius, y - radius, radius * 2, radius * 2);

            // 绘制边框（红方红色，黑方黑色）
            gc.setStroke(piece.getColor() == Color.RED ?
                Color.rgb(180, 0, 0) : Color.rgb(30, 30, 30));

            // 绘制棋子文字（楷体）
            gc.setFont(Font.font("楷体", FontWeight.BOLD, 26));
            gc.fillText(piece.getDisplayName(), textX, textY);
        }
    }

    // 坐标转换
    public Position canvasToPosition(double canvasX, double canvasY) {
        int col = (int) Math.round((canvasX - PADDING) / CELL_SIZE);
        int row = (int) Math.round((canvasY - PADDING) / CELL_SIZE);
        // 处理翻转状态
        if (flipped) {
            col = 8 - col;
            row = 9 - row;
        }
        return new Position(col, row);
    }
}
```

**JavaFX 特性运用**:
- `Canvas` + `GraphicsContext` - 自定义绘制
- 鼠标事件处理 - `setOnMouseClicked`
- 坐标系统转换 - 屏幕坐标到棋盘坐标

### 3. 游戏控制器 (GameController)

**文件位置**: `src/main/java/cn/chinesechess/ui/GameController.java`

连接 UI 层与核心引擎，处理用户交互:

```java
public class GameController implements GameEventListener {

    public GameController(IGameEngine gameEngine, BoardView boardView, EngineManager engineManager) {
        // 1. 注册游戏事件监听
        gameEngine.addEventListener(this);

        // 2. 绑定鼠标点击事件
        boardView.getCanvas().setOnMouseClicked(this::handleBoardClick);
    }

    // 处理棋盘点击
    private void handleBoardClick(MouseEvent event) {
        Position clickedPos = boardView.canvasToPosition(event.getX(), event.getY());

        if (selectedFrom == null) {
            // 第一次点击：选棋
            Piece piece = board.getPiece(clickedPos);
            if (piece != null && piece.getColor() == gameEngine.getCurrentTurn()) {
                selectedFrom = clickedPos;
                currentLegalTargets = gameEngine.getLegalTargets(clickedPos);
                boardView.setSelected(clickedPos, currentLegalTargets);
            }
        } else {
            // 第二次点击：走棋或取消
            if (currentLegalTargets.contains(clickedPos)) {
                gameEngine.makeMove(selectedFrom, clickedPos);
            }
            boardView.clearSelection();
            selectedFrom = null;
        }
    }

    // 观察者模式实现 - 游戏状态变化时更新 UI
    @Override
    public void onGameStateChanged(GameState state) {
        boardView.setBoard(state.getBoard());
        boardView.repaint();
    }

    @Override
    public void onCheck(Color attacker) {
        Platform.runLater(() -> {
            Alert alert = new Alert(Alert.AlertType.WARNING);
            alert.setContentText(attacker + "方将军！");
            alert.show();
        });
    }
}
```

**JavaFX 特性运用**:
- `MouseEvent` - 鼠标点击事件
- `Platform.runLater()` - UI 线程更新
- `Alert` - 警告对话框

### 4. 对话框组件

项目使用 JavaFX 内置对话框实现模式选择:

- `GameModeDialog` - 游戏模式选择（人人对战/人机对战、难度、先手）
- `EngineSettingsDialog` - 外部引擎配置

```java
// 示例：模式选择对话框
public class GameModeDialog extends Dialog<GameModeConfig> {

    private ChoiceBox<GameMode> modeChoice;
    private ChoiceBox<DifficultyLevel> difficultyChoice;
    private ChoiceBox<Color> firstHandChoice;

    public GameModeDialog() {
        setTitle("选择游戏模式");

        // 创建选项控件
        modeChoice = new ChoiceBox<>();
        modeChoice.getItems().addAll(GameMode.PVP, GameMode.PVE);

        difficultyChoice = new ChoiceBox<>();
        difficultyChoice.getItems().addAll(DifficultyLevel.values());

        // 设置确认按钮
        ButtonType confirmType = new ButtonType("确定", ButtonBar.ButtonData.OK_DONE);
        getDialogPane().getButtonTypes().addAll(confirmType, ButtonType.CANCEL);

        // 布局
        GridPane grid = new GridPane();
        grid.add(new Label("模式:"), 0, 0);
        grid.add(modeChoice, 1, 0);
        // ...
    }
}
```

### 5. 样式与主题

**文件位置**: `src/main/resources/css/style.css`

使用 CSS 定制 JavaFX 控件样式:

```css
/* 菜单栏样式 */
.menu-bar {
    -fx-background-color: #f5f5f5;
}

.menu-item:hover {
    -fx-background-color: #e0e0e0;
}

/* 工具栏按钮样式 */
.tool-bar .button {
    -fx-background-radius: 4;
    -fx-border-radius: 4;
    -fx-padding: 5 10;
}

.tool-bar .button:hover {
    -fx-background-color: #e0e0e0;
}

/* 棋盘容器样式 */
.board-container {
    -fx-background-color: #DEB887;
    -fx-border-color: #8B4513;
    -fx-border-width: 2;
}
```

---

## 数据层

### 1. 应用设置 (AppSettings)

**文件位置**: `src/main/java/cn/chinesechess/data/AppSettings.java`

使用 Properties 存储用户偏好设置:

```java
public class AppSettings {
    private static final String SETTINGS_FILE = "settings.properties";

    // 单例模式
    private static AppSettings instance;

    private Properties properties;

    public static AppSettings getInstance() {
        if (instance == null) {
            instance = new AppSettings();
        }
        return instance;
    }

    // 保存/加载设置
    public void save() throws IOException {
        try (FileOutputStream fos = new FileOutputStream(SETTINGS_FILE)) {
            properties.store(fos, "Chinese Chess Settings");
        }
    }

    public void load() throws IOException {
        try (FileInputStream fis = new FileInputStream(SETTINGS_FILE)) {
            properties.load(fis);
        }
    }
}
```

### 2. 棋谱管理 (GameRecordManager)

**文件位置**: `src/main/java/cn/chinesechess/data/GameRecordManager.java`

实现棋局记录的保存与加载:

```java
public class GameRecordManager {

    // 保存棋谱为文本格式
    public void saveRecord(GameRecord record, File file) throws IOException {
        try (PrintWriter writer = new PrintWriter(file)) {
            writer.println("中国象棋棋谱");
            writer.println("=".repeat(20));

            int moveNumber = 1;
            for (Move move : record.getMoves()) {
                if (move.getColor() == Color.RED) {
                    writer.printf("%d. %s", moveNumber, move.toChineseNotation());
                } else {
                    writer.printf("  %s%n", move.toChineseNotation());
                    moveNumber++;
                }
            }
        }
    }
}
```

---

## 总结

本项目展示了 JavaFX 在桌面应用开发中的完整运用:

| 特性 | 应用场景 |
|------|----------|
| `Application` | 应用入口与生命周期管理 |
| `Stage` / `Scene` | 窗口与场景管理 |
| `Canvas` | 自定义图形绘制（棋盘） |
| `MenuBar` / `ToolBar` | 菜单与工具栏 |
| `Dialog` | 模式选择对话框 |
| `Accelerator` | 键盘快捷键 |
| CSS | 样式定制 |
| 观察者模式 | UI 与引擎解耦 |
| `Platform.runLater()` | 线程安全 UI 更新 |
